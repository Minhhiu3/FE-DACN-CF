export type LoginData = {
    email: string;
    password: string;
}
export type RegisterData = {
    id?: number;
    name: string;
    email: string;
    password: string;
    confirmPassword: string
}